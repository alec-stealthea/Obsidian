The Open Group would define a capability as the ability to do somethng, a business capability is the ability for a particular organization to do something. This is an abstraction of a collection of people performing roles, using tools and processes to achieve an outcome that supports a value stream stage.

